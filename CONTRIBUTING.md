# 参与贡献

感谢你关注 vates。欢迎通过 Issue、代码、测试、文档和基准报告改进项目。

参与项目前，请阅读并遵守 [贡献者公约](CODE_OF_CONDUCT.md)。

## 提交 Issue

提交前请先搜索[现有 Issue](https://github.com/AMOS144/Vates/issues)，避免重复报告。

- Bug 报告应包含可复现步骤、设备型号、macOS 与 Python 版本、项目提交、运行命令和完整日志。
- 功能建议应说明实际使用场景、当前痛点、预期行为和可能的替代方案。
- 请删除日志中的模型路径、用户名、令牌和其他敏感信息。
- 不适合公开讨论的社区行为问题，请发送邮件至 [3108424075@qq.com](mailto:3108424075@qq.com)。

## 开发环境

项目要求 Apple Silicon Mac 和 Python 3.11 或更高版本。

```bash
# 克隆仓库
git clone https://github.com/AMOS144/Vates.git
cd Vates

# 安装运行与开发依赖
uv sync

# 激活虚拟环境
source .venv/bin/activate

# 编译生产路径使用的原生扩展
cd native/ext && make native_moe_ext && cd ../..

# 运行完整测试集
.venv/bin/python -m pytest
```

无需模型即可使用 `vates --demo` 检查 TUI。

## 分支与改动范围

请从最新的 `main` 创建独立分支。推荐使用以下前缀：

- `feat/`：新功能
- `fix/`：Bug 修复
- `perf/`：性能优化
- `docs/`：文档
- `test/`：测试
- `refactor/`：不改变行为的重构

一个 Pull Request 应只解决一个明确问题。请勿混入无关格式化、重命名或重构。

## 代码规范

- 遵循现有模块边界和代码风格。
- **代码注释必须使用中文**，并解释设计意图而非重复代码行为。
- 新增或修改行为时，应补充对应测试。
- 不提交模型权重、构建产物、个人配置、凭据或大体积临时文件。
- 不以降低正确性为代价换取性能。

## 测试与验证

提交前至少运行与改动直接相关的测试。条件允许时运行完整测试集：

```bash
.venv/bin/python -m pytest
```

涉及专家池、预取、MTP、量化或原生扩展时，还应提供：

1. 可直接复现的命令和环境变量；
2. 修改前后的同条件数据；
3. 设备、系统和模型配置；
4. 容量不变性或逐字节真值校验结果；
5. 吞吐、内存和质量之间的取舍说明。

如果完整测试中存在与本次改动无关的既有失败，请在 Pull Request 中列出失败项和判断依据，不要隐瞒。

## Commit

提交消息应简洁说明改动目的。推荐格式：

```text
<类型>(<范围>): <摘要>
```

例如：

```text
fix(pool): 修复非连续专家索引的错槽问题
docs(readme): 补充模型准备说明
```

请勿跳过仓库钩子，也不要将多个无关改动压入同一提交。

## Pull Request

Pull Request 应包含：

- 改动背景与目标；
- 主要实现方式；
- 关联 Issue；
- 已执行的验证命令和结果；
- 性能、内存、正确性及兼容性影响；
- 用户可见行为变化对应的文档更新；
- 已知限制和后续工作。

维护者可能要求补充测试、缩小改动范围或提供更多基准数据。提交即表示你同意贡献内容按照项目的 [Apache License 2.0](LICENSE) 发布。
